image: node:8.17.0

stages:
  - ver
  - tests
  - deploy

ver:
  stage: ver
  script:
    - node --version
    - whoami

run_test:
  stage: tests
  script:
    - echo "Add tests later"

deploy:
  stage: deploy
  script:
    - npm install
    - npm install --prefix ./functions
    - npm run build
    - npm run deploy_production
  only:
    - master